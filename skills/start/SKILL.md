---
name: start
description: "🧭 Vela 파이프라인 바로 시작 — /vela:start 슬래시 명령어로 위임합니다."
---

# /vela:start — 슬래시 명령어로 위임

이 스킬은 `/vela:start` 슬래시 명령어로 위임한다.

`~/.claude/commands/vela/start.md`를 통해 `.vela/agents/vela.md`의 start 절차가 실행된다.

작업 내용이 있으면 그대로 전달한다: `$ARGUMENTS`
